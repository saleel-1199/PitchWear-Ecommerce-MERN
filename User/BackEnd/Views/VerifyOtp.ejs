<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Enter OTP</title>

  <!-- Tailwind CDN -->
  <script src="https://cdn.tailwindcss.com"> </script>
  <script>
  document.addEventListener("DOMContentLoaded", () => {
    const inputs = document.querySelectorAll(".otp-input");

    console.log("OTP inputs found:", inputs.length); // should be 6

    inputs.forEach((input, index) => {
      input.addEventListener("input", () => {
        if (input.value && index < inputs.length - 1) {
          inputs[index + 1].focus();
        }
      });

      input.addEventListener("keydown", (e) => {
        if (e.key === "Backspace" && !input.value && index > 0) {
          inputs[index - 1].focus();
        }
      });
    });
  });
</script>

</head>
<body class="bg-[#faf9f7] min-h-screen flex items-center justify-center">

  <div class="w-full max-w-xl bg-white px-16 py-14">

    <!-- Back -->
    <div class="flex items-center gap-2 mb-8 cursor-pointer">
      <div class="border border-black p-1">
        ←
      </div>
      <span class="text-sm font-medium">Back</span>
    </div>

    <!-- Heading -->
    <h1 class="text-3xl font-bold mb-2">Enter<br>OTP</h1>
    <p class="text-gray-500 mb-10">
      Enter the 6-digit code sent to your email
    </p>

    <!-- OTP Inputs -->
    <form action="/VerifyOtp" method="POST">
      <div class="flex gap-4 mb-6">
        <% for(let i = 0; i < 6; i++) { %>
          <input
            type="text"
            maxlength="1"
            name="otp[]"
            class="otp-input w-12 h-14 text-center border border-gray-400 text-lg focus:outline-none focus:border-black"
          />
        <% } %>
      </div>

      <!-- Timer & Resend -->
      <div class="flex justify-between items-center text-sm text-gray-500 mb-8">
        <span>Expires in 00 : 00</span>
        <button type="button" class="font-medium text-black hover:underline">
          Resend Code
        </button>
      </div>

      <!-- Verify Button -->
      <button
        type="submit"
        class="w-full bg-[#d6c87a] text-white py-4 font-medium flex items-center justify-center gap-2 hover:opacity-90 transition"
      >
        Verify →
      </button>
    </form>

    <!-- Footer -->
    <div class="text-center mt-8 text-sm text-gray-500">
      Having trouble?
      <a href="#" class="text-black font-medium hover:underline">
        Contact Support
      </a>
    </div>

  </div>

</body>
</html>
